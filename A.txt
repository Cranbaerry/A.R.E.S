3rtwrozcspjeuqjdxu32zhluh56ohxnzadj7yggmn2x5kurs5lpcpwyd.onion
cd /var/www/html
sudo nano /etc/rc.local

# Print the IP address
_IP=$(hostname -I) || true
if [ "$_IP" ]; then
  printf "My IP address is %s\n" "$_IP"
  ./boot.sh
fi

exit 0
